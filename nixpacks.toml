[phases.setup]
nixPkgs = ["nodejs", "python3"]

[phases.install]
cmds = ["npm install"]

# Frontend is deployed separately - no build phase needed for backend
# [phases.build]
# cmds = [
#   "rm -rf frontend/dist",
#   "npm run build:frontend"
# ]

[start]
cmd = "node start.js"
